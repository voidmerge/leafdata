{
  "name": "@leaf/leaf-data",
  "version": "0.0.1",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "gen": "antlr-ng -Dlanguage=TypeScript -visitor -o ./src/gen ./leafdataLexer.g4 ./leafdataParser.g4",
    "fmt": "prettier . --write",
    "build": "npm run gen && tsc --build",
    "test": "npm run build && vitest run"
  },
  "devDependencies": {
    "antlr-ng": "^1.0.10",
    "prettier": "^3.9.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "antlr4ng": "^3.0.16"
  }
}
